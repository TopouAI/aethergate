# OpenMeter Integration

Planned phase-three integration for meters, credits, entitlements, quotas, and billing events.

AetherGate owns customer-facing enterprise structure and contract context. OpenMeter receives normalized, idempotent usage events and returns metering state through an adapter. The initial foundation deployment does not require OpenMeter.

