# LiteLLM Deployment Configuration

Reusable LiteLLM configuration belongs here when it is shared across deployment targets. The initial single-server `litellm-config.yaml` may stay under `deploy/compose/core` because it is composed and tested with that stack.

