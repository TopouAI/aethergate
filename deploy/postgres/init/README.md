# PostgreSQL Initialization

Reusable initialization sources belong here when they are shared by more than one deployment target. The current stack's `01-create-databases.sh` may remain under `deploy/compose/core/postgres/init` while it is specific to that Compose deployment.

