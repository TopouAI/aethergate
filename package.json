{
  "name": "aethergate",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/console"
  ],
  "scripts": {
    "dev": "cd apps/console && next dev",
    "build": "cd apps/console && next build",
    "lint": "cd apps/console && eslint .",
    "typecheck": "cd apps/console && tsc --noEmit"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "npm@11.12.1"
}
