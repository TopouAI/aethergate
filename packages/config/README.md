# Configuration Package

Shared repository configuration for TypeScript, formatting, linting, test conventions, and build tooling. Go formatting and analysis should use standard Go tooling plus explicitly documented checks.
